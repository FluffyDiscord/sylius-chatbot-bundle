{% set widget = hookable_metadata is defined ? hookable_metadata.context : _context %}
<script src="{{ widget.widget_cdn_url }}" defer></script>
<ai-chat-widget site-key="{{ widget.site_key }}" locale="{{ app.locale }}" backend-url="{{ widget.backend_url }}"></ai-chat-widget>
